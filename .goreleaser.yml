version: 2
project_name: pando
before:
  hooks:
builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      # PANDO_BETTERSTACK_TOKEN embeds the Better Stack ingest source token
      # (see internal/telemetry/build.go and the Makefile's
      # TELEMETRY_LDFLAGS). envOrDefault never errors when the env var is
      # unset, unlike `.Env.PANDO_BETTERSTACK_TOKEN`, so a goreleaser run
      # without the secret still builds, just with telemetry unavailable.
      - -s -w -X github.com/digiogithub/pando/internal/version.Version={{.Version}} -X github.com/digiogithub/pando/internal/telemetry.sourceToken={{ envOrDefault "PANDO_BETTERSTACK_TOKEN" "" }}
    main: ./main.go

archives:
  - format: tar.gz
    name_template: >-
      pando-
      {{- if eq .Os "darwin" }}mac-
      {{- else if eq .Os "windows" }}windows-
      {{- else if eq .Os "linux" }}linux-{{end}}
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "#86" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip
checksum:
  name_template: "checksums.txt"
snapshot:
  name_template: "0.0.0-{{ .Timestamp }}"
nfpms:
  - maintainer: José F. Rives
    description: terminal based AI agent for software development
    formats:
      - deb
      - rpm
    file_name_template: >-
      {{ .ProjectName }}-
      {{- if eq .Os "darwin" }}mac
      {{- else }}{{ .Os }}{{ end }}-{{ .Arch }}

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^doc:"
      - "^test:"
      - "^ci:"
      - "^ignore:"
      - "^example:"
      - "^wip:"
