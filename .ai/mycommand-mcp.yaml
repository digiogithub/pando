tools:
  - name: "osx-shell"
    description: "Execute commands in an OSX zsh shell environment, send all commands to run into input argument"
    command: ".ai/scripts/osx-connection.sh"
    path: "/www/MCP/Pando/pando/"
    accepts_args: false
    accept_input: true
