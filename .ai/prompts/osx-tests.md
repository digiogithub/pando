You have an `osx-shell` tool to execute commands in a zsh shell on an osx machine. You will arrive at a path where the pando code is synchronized by git, but you can edit files on the fly for testing. Review how it works in the Makefile and the task in the Readme for building the dist for osx, which connects via SSH to create the .pkg for osx. It seems the package is not signed because osx displays an error when executing the pkg.
