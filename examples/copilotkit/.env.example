# Origin of the Pando AG-UI adapter (pando agui-serve --port 8090)
PANDO_URL=http://localhost:8090

# Bearer token printed by `pando agui-serve` at startup. It stays server-side:
# only the Next.js route reads it, never the browser.
PANDO_TOKEN=replace-me
