# Pando MCP Gateway, Lua Hooks & Tool Favorites

This document outlines the implementation plan for integrating a **MCP Gateway** and optional **Lua hook** support into the *Panda* codebase.  The goal is to expose all configured MCP servers under a single gateway that serves generic tools (`mcp_query_catalog`, `mcp_call_tool`) while providing lightweight filtering, statistics & favorite‑handling.

The plan follows four phases – each with concrete deliverables and integration points:

## Phase 1 – Core MCP Gateway + Lua Infrastructure
+… (rest of content)