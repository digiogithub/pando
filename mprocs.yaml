procs:
  #  mesnada: "mesnada -config .ai/mesnada.config.yaml"
  # remembrances: "remembrances-mcp-beta --config .ai/remembrances.config.yaml"
  web-ui: "cd web-ui && npm install && VITE_API_BASE_URL=https://localhost:8888 npm run dev"
  pando-serve: "./pando serve --port 8888"
