# Pando HTTP API Routes

The API server registers routes for health, token issuance, project info, project context, sessions, per-session access, tools, files, per-file access, chat, and chat streaming endpoints. This confirms a backend/web integration surface beyond the terminal UI.