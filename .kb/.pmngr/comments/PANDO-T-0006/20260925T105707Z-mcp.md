---
type: comment
item: PANDO-T-0006
author: mcp
created: 2026-09-25T10:57:07Z
kind: comment
---

Implemented clean-checkout embedding support and verified acceptance criteria. `go build ./...` succeeds in an isolated snapshot created from tracked files only (no prior generated build assets). `internal/api/ui_assets_app.go` embeds `webui/**`, serving built `webui/dist` when present or a tracked placeholder otherwise. Desktop embeds `bin/**` with tracked placeholders; generated binary and macOS bundle assets are used if present and remain unmodified. README now documents clean-clone build commands. Verification: clean-snapshot `go build ./...` passed; `go test ./internal/api ./cmd ./internal/desktop` passed with isolated HOME; `git diff --check` passed. macOS cross-build not validated because the repository's tree-sitter dependencies require platform/CGo support.
